document.head.insertAdjacentHTML('beforeend','<style>.task .edit-task{min-height:30px!important;width:30px!important;padding:0!important;border-radius:8px!important;background:var(--t)!important;color:var(--v)!important;font-size:.9rem!important;justify-self:end}.task .priority-note{display:block;margin-top:4px;font-size:.76rem;color:var(--m)}</style>');
const extras=document.createElement('section');extras.className='card';extras.innerHTML='<div class="row"><select id="prio"><option value="normal">🟡 Normal</option><option value="high">🔴 Viktigt</option><option value="low">🟢 När tid finns</option></select></div><div class="row" style="margin-top:9px"><input id="find" placeholder="🔎 Sök i uppgifter"><select id="filter"><option value="">Alla prioriteter</option><option value="high">🔴 Viktigt</option><option value="normal">🟡 Normal</option><option value="low">🟢 När tid finns</option></select></div>';document.querySelector('.card').after(extras);
T.forEach(task=>delete task.repeat);save();
$('#form').onsubmit=e=>{e.preventDefault();let text=$('#input').value.trim();if(!text)return;T.unshift({id:crypto.randomUUID(),text,done:false,marked:false,priority:$('#prio').value});save();e.target.reset();render()};
const baseRender=render;render=function(){baseRender();document.querySelectorAll('.task').forEach(li=>{let task=T.find(x=>x.text===li.querySelector('.text').textContent);if(!task)return;let edit=document.createElement('button');edit.className='edit-task';edit.title='Redigera uppgift';edit.textContent='✏️';edit.onclick=()=>{let changed=prompt('Redigera uppgift',task.text);if(changed&&changed.trim()){task.text=changed.trim();save();render()}};li.append(edit);let note=document.createElement('small');note.className='priority-note';note.textContent=task.priority==='high'?'🔴 Viktigt':task.priority==='low'?'🟢 När tid finns':'🟡 Normal';li.querySelector('.text').after(note)});let search=$('#find').value.toLowerCase();document.querySelectorAll('.task').forEach(li=>li.hidden=Boolean(search)&&!li.textContent.toLowerCase().includes(search))};
function filterTasks(){let search=$('#find').value.toLowerCase(),priority=$('#filter').value;document.querySelectorAll('.task').forEach(li=>{let task=T.find(x=>x.text===li.querySelector('.text').textContent),show=(!search||li.textContent.toLowerCase().includes(search))&&(!priority||task.priority===priority);li.hidden=!show})}$('#find').oninput=filterTasks;$('#filter').onchange=filterTasks;render();
